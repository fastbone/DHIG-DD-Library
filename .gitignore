# index, text mirror, generated deliverables — all rebuildable from the corpus
data/
sample-dataroom/

# compose bind-mount target: keep the folder, ignore whatever is dropped in it
corpus/*
!corpus/.gitkeep

__pycache__/
*.py[cod]
.venv/
venv/
.env

.DS_Store
*.swp
