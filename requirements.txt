anthropic>=0.121.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9   # multipart archive upload
cryptography>=41         # AES-GCM for API keys at rest

# extraction
pymupdf>=1.24          # PDF text + optional OCR textpage
python-pptx>=0.6.23    # slides + speaker notes
openpyxl>=3.1          # spreadsheet values and formulas
python-docx>=1.1       # Word documents

# optional, for run_python analysis over original workbooks
pandas>=2.0
